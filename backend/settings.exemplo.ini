# Configurações da aplicação — MODO LOCAL (padrão)
# Copie este arquivo para settings.ini e preencha com os valores reais.
# NUNCA commite o settings.ini no git.

# --SQL--
# PostgreSQL local
sql.url=jdbc:postgresql://localhost:5432/DeereMail
sql.user=postgres
sql.password=1234
